import React from "react";

const Header = () => {
    return (
        <header className="text-center border-bottom border-4 border-dark">
            <h1> Welcome to Valor! </h1>
        </header>

    );
};

export default Header;